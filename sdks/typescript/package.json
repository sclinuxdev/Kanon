{
  "name": "@kanon/sdk-and-host",
  "version": "0.1.0",
  "description": "Kanon TypeScript SDK and Host process for out-of-process plugins and platform adapters",
  "main": "dist/src/sdk/index.js",
  "types": "dist/src/sdk/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/tests"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.10"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
