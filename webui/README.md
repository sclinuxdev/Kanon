# Kanon WebUI (Decoupled Frontend)

This directory is reserved for the independent WebUI console (e.g. Vue 3 / React + Vite SPA).
It communicates purely with the Rust backend via the RESTful API and WebSocket endpoints provided by `kanon-api`.
